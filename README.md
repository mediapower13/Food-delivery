# Chuks Kitchen – Frontend (React + Vite + Tailwind)

## Setup
1) Install Node.js (LTS recommended)
2) In this folder, run:

```bash
npm install
npm run dev
```

Open: http://localhost:5173

## Add your Figma images
Place exported images in:
`public/images/`

Then update `src/data/foods.js` to match your menu items and image filenames.
# Food-delivery
